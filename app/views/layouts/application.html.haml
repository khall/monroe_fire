!!!
%html
  %head
    %title Monroe Rural Fire Protection District
    %meta{name: "description", content:"Current run information for all emergencies responded to by our department"}
    -#= stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
    :javascript
      var _gaq = _gaq || [];
      _gaq.push(['_setAccount', 'UA-39433949-1']);
      _gaq.push(['_trackPageview']);

      (function() {
        var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
        ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
        var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
      })();
    :css
      body {
        font-family: helvetica, arial, sans-serif;
        line-height: 16px;
        background-color: ivory; }

      #main {
        margin: 0 auto;
        width: 850px;
        background-color: white;
        border: 2px solid lightgrey;
        border-radius: 5px 5px 5px 5px;
        -moz-border-radius: 5px 5px 5px 5px; }

      #content {
        padding: 20px; }

      #header_img {
        height: 264px; }

      #page_footer {
        border-top: 1px solid lightgrey;
        padding: 15px; }

      .navbar {
        padding: 0;
        margin-top: 0;
        background-color: #011B7D; }
        .navbar a {
          text-decoration: none; }
        .navbar li {
          display: inline-block;
          padding: 5px;
          border-right: 3px solid white;
          color: white;
          font-size: 15px;
          font-weight: bold;
          line-height: 25px;  }
        .navbar li:hover {
          background-color: #022CCB; }
  %body
    #main
      = render 'common/header'
      #content
        = yield
      = render 'common/footer'